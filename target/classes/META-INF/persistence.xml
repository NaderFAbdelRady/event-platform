<?xml version="1.0" encoding="UTF-8"?>
<persistence version="2.2"
             xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd">

    <persistence-unit name="PlatformPU" transaction-type="JTA">

        <jta-data-source>java:/jdbc/PlatformDS</jta-data-source>

        <!-- ✅ Register the new ComponentDefinition entity -->
        <class>com.platform.engine.ComponentDefinition</class>

        <properties>
            <!--
                ✅ FIX: Changed from "create" / "create-drop" to "update".

                "create"      → drops and recreates ALL tables on every deploy  ← was wiping your data
                "create-drop" → same as create, also drops tables on shutdown   ← same problem
                "update"      → only adds missing tables/columns, KEEPS all data ← correct for production
                "validate"    → checks schema but never modifies it
                "none"        → does nothing (use when you manage schema manually)
            -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- MySQL 8 dialect -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/>

            <!-- Optional: show SQL in logs during development (set to false in production) -->
            <property name="hibernate.show_sql" value="false"/>
            <property name="hibernate.format_sql" value="false"/>

        </properties>

    </persistence-unit>

</persistence>
